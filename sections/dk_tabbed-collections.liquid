{% comment %} Added by DK on 2026-05-07 — Tabbed collections section (Dossier-style) {% endcomment %}
{% comment %} Updated by DK on 2026-05-07 — Tabs moved to section settings (no schema blocks) to allow content_for product-card {% endcomment %}

{% liquid
  comment
    Tabs are driven by section settings (tab_1_* … tab_6_*) rather than blocks.
    This is required because content_for 'block' (theme blocks) and schema-defined
    blocks cannot coexist in the same section.
  endcomment
  assign max_tabs = 6
%}

<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div
  class="dk-tabs-section color-{{ section.settings.color_scheme }} spacing-style"
  style="{%- render 'spacing-style', settings: section.settings -%}"
>

  {%- if section.settings.heading != blank -%}
    <p class="dk-tabs-section__eyebrow">{{ section.settings.heading }}</p>
  {%- endif -%}

  {%- comment -%} ── TAB BAR ── {%- endcomment -%}
  {%- assign first_tab = true -%}
  {%- assign first_tab_idx = 0 -%}
  <div class="dk-tabs-section__tabs" role="tablist">
    {%- for i in (1..max_tabs) -%}
      {%- assign col_key   = 'tab_' | append: i | append: '_collection' -%}
      {%- assign label_key = 'tab_' | append: i | append: '_label' -%}
      {%- assign img_key   = 'tab_' | append: i | append: '_image' -%}
      {%- assign tab_col   = section.settings[col_key] -%}
      {%- assign tab_label = section.settings[label_key] -%}
      {%- assign tab_img   = section.settings[img_key] -%}
      {%- if tab_col != blank -%}
        {%- if first_tab -%}{%- assign first_tab_idx = i -%}{%- endif -%}
        <button
          class="dk-tabs-section__tab{% if first_tab %} is-active{% endif %}"
          onclick="dkTabsSwitch('{{ section.id }}', {{ i }})"
          id="dkTab-{{ section.id }}-{{ i }}"
          role="tab"
          aria-selected="{% if first_tab %}true{% else %}false{% endif %}"
          aria-controls="dkPanel-{{ section.id }}-{{ i }}"
        >
          {%- if tab_img != blank -%}
            <span class="dk-tabs-section__tab-img-wrap">
              {{- tab_img | image_url: width: 120 | image_tag: loading: 'lazy', widths: '80, 120', class: 'dk-tabs-section__tab-img' -}}
            </span>
          {%- endif -%}
          <span class="dk-tabs-section__tab-label">{{ tab_label | default: tab_col.title }}</span>
        </button>
        {%- assign first_tab = false -%}
      {%- endif -%}
    {%- endfor -%}
  </div>

  {%- comment -%} ── PANELS — content_for renders native product cards ── {%- endcomment -%}
  {%- assign max_products = section.settings.max_products -%}
  {%- for i in (1..max_tabs) -%}
    {%- assign col_key        = 'tab_' | append: i | append: '_collection' -%}
    {%- assign view_all_key   = 'tab_' | append: i | append: '_view_all_url' -%}
    {%- assign view_text_key  = 'tab_' | append: i | append: '_view_all_text' -%}
    {%- assign tab_col        = section.settings[col_key] -%}
    {%- assign tab_view_url   = section.settings[view_all_key] -%}
    {%- assign tab_view_text  = section.settings[view_text_key] -%}
    {%- if tab_col != blank -%}
      <div
        class="dk-tabs-section__panel{% if i == first_tab_idx %} is-active{% endif %}"
        id="dkPanel-{{ section.id }}-{{ i }}"
        role="tabpanel"
        aria-labelledby="dkTab-{{ section.id }}-{{ i }}"
      >
        <div
          class="resource-list resource-list--grid"
          style="
            --resource-list-columns: repeat({{ section.settings.columns }}, 1fr);
            --resource-list-columns-mobile: repeat({{ section.settings.mobile_columns }}, 1fr);
            --resource-list-column-gap-desktop: {{ section.settings.columns_gap }}px;
            --resource-list-row-gap-desktop: {{ section.settings.rows_gap }}px;
          "
        >
          {%- for product in tab_col.products limit: max_products -%}
            <div class="resource-list__item">
              {%- content_for 'block', type: 'product-card', id: 'static-product-card', closest.product: product -%}
            </div>
          {%- endfor -%}
        </div>

        {%- assign footer_url = tab_view_url | default: tab_col.url -%}
        {%- if footer_url != blank -%}
          <div class="dk-tabs-section__footer">
            <a href="{{ footer_url }}" class="dk-tabs-section__view-all">
              {{- tab_view_text | default: section.settings.view_all_text -}}
            </a>
          </div>
        {%- endif -%}
      </div>
    {%- endif -%}
  {%- endfor -%}

</div>

{% stylesheet %}
/* Added by DK on 2026-05-07 — dk_tabbed-collections */
.dk-tabs-section {
  padding: 60px 72px;
}

/* Eyebrow */
.dk-tabs-section__eyebrow {
  font-size: 10px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  opacity: 0.5;
  margin: 0 0 28px;
}

/* ── TAB BAR ── */
.dk-tabs-section__tabs {
  display: flex;
  gap: 0;
  border-bottom: 1px solid rgba(0,0,0,0.1);
  margin-bottom: 36px;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
}
.dk-tabs-section__tabs::-webkit-scrollbar { display: none; }

.dk-tabs-section__tab {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 14px 28px 14px 0;
  margin-right: 36px;
  background: none;
  border: none;
  border-bottom: 2px solid transparent;
  cursor: pointer;
  color: rgba(0,0,0,0.4);
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  white-space: nowrap;
  transition: color 0.2s, border-color 0.2s;
  margin-bottom: -1px;
}
.dk-tabs-section__tab:hover { color: rgba(0,0,0,0.7); }
.dk-tabs-section__tab.is-active {
  color: #1a1a18;
  border-bottom-color: #1a1a18;
}

/* Tab image thumbnail */
.dk-tabs-section__tab-img-wrap {
  width: 52px;
  height: 40px;
  border-radius: 6px;
  overflow: hidden;
  flex-shrink: 0;
  background: #f0ede8;
}
.dk-tabs-section__tab-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

/* ── PANELS ── */
.dk-tabs-section__panel { display: none; }
.dk-tabs-section__panel.is-active { display: block; }

/* View all footer */
.dk-tabs-section__footer {
  margin-top: 32px;
  text-align: center;
}
.dk-tabs-section__view-all {
  display: inline-block;
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #1a1a18;
  text-decoration: none;
  border-bottom: 1px solid #1a1a18;
  padding-bottom: 2px;
  transition: opacity 0.2s;
}
.dk-tabs-section__view-all:hover { opacity: 0.6; }

/* ── RESPONSIVE ── */
@media (max-width: 768px) {
  .dk-tabs-section { padding: 44px 20px; }
  .dk-tabs-section__tab { padding: 12px 20px 12px 0; margin-right: 20px; font-size: 10px; }
  .dk-tabs-section__tab-img-wrap { width: 40px; height: 32px; }
}
{% endstylesheet %}

<script>
/* Added by DK on 2026-05-07 */
function dkTabsSwitch(sectionId, n) {
  var tabPrefix   = 'dkTab-'   + sectionId + '-';
  var panelPrefix = 'dkPanel-' + sectionId + '-';
  document.querySelectorAll('[id^="' + tabPrefix + '"]').forEach(function(el) {
    var active = el.id === tabPrefix + n;
    el.classList.toggle('is-active', active);
    el.setAttribute('aria-selected', active ? 'true' : 'false');
  });
  document.querySelectorAll('[id^="' + panelPrefix + '"]').forEach(function(el) {
    el.classList.remove('is-active');
  });
  var panel = document.getElementById(panelPrefix + n);
  if (panel) panel.classList.add('is-active');
}
</script>

{% schema %}
{
  "name": "DK Tabbed Collections",
  "tag": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Eyebrow label",
      "default": "Shop our collections"
    },
    {
      "type": "header",
      "content": "Tab 1"
    },
    {
      "type": "text",
      "id": "tab_1_label",
      "label": "Label",
      "default": "Bestsellers"
    },
    {
      "type": "collection",
      "id": "tab_1_collection",
      "label": "Collection"
    },
    {
      "type": "image_picker",
      "id": "tab_1_image",
      "label": "Tab image"
    },
    {
      "type": "url",
      "id": "tab_1_view_all_url",
      "label": "\"View all\" URL"
    },
    {
      "type": "text",
      "id": "tab_1_view_all_text",
      "label": "\"View all\" text"
    },
    {
      "type": "header",
      "content": "Tab 2"
    },
    {
      "type": "text",
      "id": "tab_2_label",
      "label": "Label",
      "default": "New Releases"
    },
    {
      "type": "collection",
      "id": "tab_2_collection",
      "label": "Collection"
    },
    {
      "type": "image_picker",
      "id": "tab_2_image",
      "label": "Tab image"
    },
    {
      "type": "url",
      "id": "tab_2_view_all_url",
      "label": "\"View all\" URL"
    },
    {
      "type": "text",
      "id": "tab_2_view_all_text",
      "label": "\"View all\" text"
    },
    {
      "type": "header",
      "content": "Tab 3"
    },
    {
      "type": "text",
      "id": "tab_3_label",
      "label": "Label",
      "default": "Staff Picks"
    },
    {
      "type": "collection",
      "id": "tab_3_collection",
      "label": "Collection"
    },
    {
      "type": "image_picker",
      "id": "tab_3_image",
      "label": "Tab image"
    },
    {
      "type": "url",
      "id": "tab_3_view_all_url",
      "label": "\"View all\" URL"
    },
    {
      "type": "text",
      "id": "tab_3_view_all_text",
      "label": "\"View all\" text"
    },
    {
      "type": "header",
      "content": "Tab 4"
    },
    {
      "type": "text",
      "id": "tab_4_label",
      "label": "Label"
    },
    {
      "type": "collection",
      "id": "tab_4_collection",
      "label": "Collection"
    },
    {
      "type": "image_picker",
      "id": "tab_4_image",
      "label": "Tab image"
    },
    {
      "type": "url",
      "id": "tab_4_view_all_url",
      "label": "\"View all\" URL"
    },
    {
      "type": "text",
      "id": "tab_4_view_all_text",
      "label": "\"View all\" text"
    },
    {
      "type": "header",
      "content": "Tab 5"
    },
    {
      "type": "text",
      "id": "tab_5_label",
      "label": "Label"
    },
    {
      "type": "collection",
      "id": "tab_5_collection",
      "label": "Collection"
    },
    {
      "type": "image_picker",
      "id": "tab_5_image",
      "label": "Tab image"
    },
    {
      "type": "url",
      "id": "tab_5_view_all_url",
      "label": "\"View all\" URL"
    },
    {
      "type": "text",
      "id": "tab_5_view_all_text",
      "label": "\"View all\" text"
    },
    {
      "type": "header",
      "content": "Tab 6"
    },
    {
      "type": "text",
      "id": "tab_6_label",
      "label": "Label"
    },
    {
      "type": "collection",
      "id": "tab_6_collection",
      "label": "Collection"
    },
    {
      "type": "image_picker",
      "id": "tab_6_image",
      "label": "Tab image"
    },
    {
      "type": "url",
      "id": "tab_6_view_all_url",
      "label": "\"View all\" URL"
    },
    {
      "type": "text",
      "id": "tab_6_view_all_text",
      "label": "\"View all\" text"
    },
    {
      "type": "header",
      "content": "Products"
    },
    {
      "type": "range",
      "id": "max_products",
      "label": "Products per tab",
      "min": 4,
      "max": 16,
      "step": 4,
      "default": 8
    },
    {
      "type": "text",
      "id": "view_all_text",
      "label": "Default \"View all\" text",
      "default": "View all →"
    },
    {
      "type": "header",
      "content": "Grid"
    },
    {
      "type": "range",
      "id": "columns",
      "label": "Columns (desktop)",
      "min": 2,
      "max": 5,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "mobile_columns",
      "label": "Columns (mobile)",
      "min": 1,
      "max": 3,
      "step": 1,
      "default": 2
    },
    {
      "type": "range",
      "id": "columns_gap",
      "label": "Column gap",
      "min": 8,
      "max": 48,
      "step": 4,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "rows_gap",
      "label": "Row gap",
      "min": 8,
      "max": 64,
      "step": 4,
      "unit": "px",
      "default": 32
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 60
    }
  ],
  "presets": [
    {
      "name": "DK Tabbed Collections",
      "settings": {
        "tab_1_label": "Bestsellers",
        "tab_2_label": "New Releases",
        "tab_3_label": "Staff Picks"
      }
    }
  ]
}
{% endschema %}
